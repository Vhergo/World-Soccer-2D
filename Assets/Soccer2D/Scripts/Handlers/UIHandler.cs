using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UIHandler : MonoBehaviour
{
    //public static UIHandler Instance { get; private set; }

    //#region UI ELEMENTS
    //[Header("Position Tweens")]
    //[SerializeField] private TweenPosition menuButton;
    //[SerializeField] private TweenPosition menuButton4Player;
    //[SerializeField] private TweenPosition BlueGoal;
    //[SerializeField] private TweenPosition RedGoal;

    //[Header("Alpha Tweens")]
    //[SerializeField] private TweenAlpha pausePanel;
    //[SerializeField] private TweenAlpha blueJumpButton;
    //[SerializeField] private TweenAlpha redJumpButton;
    //[SerializeField] private TweenAlpha MenuButton;
    //[SerializeField] private TweenAlpha MenuButton4Player;
    //[SerializeField] private TweenAlpha blueJumpButton1;
    //[SerializeField] private TweenAlpha redJumpButton1;

    //#endregion

    //private void Awake()
    //{
    //    if (Instance == null) Instance = this;
    //    else Destroy(gameObject);
    //}
}
